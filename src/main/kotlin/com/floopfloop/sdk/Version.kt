package com.floopfloop.sdk

/**
 * Library semver, kept in sync with the latest `v*` git tag.
 * The release workflow asserts this matches `${GITHUB_REF_NAME#v}`.
 */
public object FloopFloopSDK {
    public const val VERSION: String = "0.1.0-alpha.1"
}
